# 标准 Python Agent Skill 库 模板详细说明

> **原则性规范**（能力域拆分、强制约束、禁止行为）见 [`Claude.md`](Claude.md)。本文档只提供可直接复制复用的文件模板与示例代码。

## 一、整体目录结构（推荐模板）

```
skill-name/                # 技能根目录（命名：短横线 kebab-case，如 file-parser）
├── SKILL.md               # Agent 技能描述文件（必选，AI 识别核心）
├── scripts/               # AI 调用脚本目录（官方指定，不可改名）
│   └── main.py            # AI 唯一入口脚本（固定文件名，不可修改）
├── evals/                 # AI 技能评测目录（必选，用于验证 AI 调用稳定性）
│   ├── evals.json         # 评测用例配置（必选，基础模板可直接填空）
│   └── files/             # 评测样本文件目录（可选，存放测试用素材）
├── skill_name/            # Python 原生源码包（命名：下划线 snake_case，如 file_parser）
│   ├── __init__.py        # 导出对外暴露的函数（必选，简化外部导入）
│   ├── core.py            # 共享基础设施（加载、校验、错误处理、类型定义）
│   ├── feature_a.py       # 子功能 A（可选，如 split）
│   └── feature_b.py       # 子功能 B（可选，如 merge）
├── examples/              # 开发者使用示例目录（可选，逐步沉淀场景）
├── tests/                 # 单元测试目录（必选，保障业务逻辑正确性）
├── pyproject.toml         # Python 包配置文件（必选，支持本地安装和发布）
├── README.md              # 开发者使用文档（必选，面向普通开发者）
└── references/            # 可选：长说明、错误码表、大段 API 摘录；须在 SKILL.md 某一章正文内写明「何时再读」
```

**注**：上表为推荐模板。当 skill 覆盖多个子功能时，`skill_name/` 下可按功能拆分为多个模块；`scripts/main.py` 可通过参数 `--action` 或 `argparse` 子命令暴露多入口。固定不可变动的部分：`SKILL.md`、`scripts/main.py`、`evals/`、`pyproject.toml`、`README.md` 的文件名与相对位置。

## 二、每个文件详细说明&填充模板
### 1. SKILL.md（必选，AI 识别核心）

#### YAML `description` 字段（Agent 判断是否加载本 skill 的主依据）
撰写与迭代请遵循 agentskills.io：[Optimizing skill descriptions](https://agentskills.io/skill-creation/optimizing-descriptions)；字段上限与格式见 [specification#description-field](https://agentskills.io/specification#description-field)。

- **祈使 + 用户意图**：用「当用户要……时使用本技能」类句式，少写实现细节。
- **边界与 near-miss**：写清「不做……」「与相邻能力区分」，并与下方「能力边界」章节一致。
- **隐式场景**：即使用户未说「Skill」「评测」等词，只要任务落在本能力域，仍应能被描述覆盖。
- **长度**：全文（含换行折叠）≤1024 字符。

**坏示例**：`处理 PDF 的工具`（过泛，无触发信息）。

**单功能好示例**：`当用户需要从本地文本文件读取内容并清洗首尾空白时使用本技能；不处理二进制文件、不解析 Markdown 语法。`

**多功能域好示例（示意，勿照抄关键词）**：`当用户需要对本地 PDF 进行页面拆分、多文件合并或文本提取时使用本技能；不提供 OCR、PDF 生成、加密解密、批量网络下载。若仅需查看元数据或页数，优先用通用文档元数据技能。`

#### 填充模板（直接复制，替换 [] 内内容即可）
```markdown
---
name: [skill-name]          # 必须与技能根目录名一致（短横线命名）
description: >
  [多行或单行：祈使句式说明何时使用本技能；写清不做的事；≤1024 字符；与「能力边界」一致]
version: 0.1.0              # 版本号，初期固定为 0.1.0，后续迭代升级
tags: ["python", "工具", "[功能标签，如文件处理、文本解析]"]
compatibility:
  python: ">=3.10"          # 固定 Python 版本要求，不修改
---

# 技能概述
简要说明本 Skill 的核心能力、适用场景、解决的具体问题，1-2 段即可，无需冗余。
示例：读取本地普通文本文件，自动适配 UTF-8 编码，可选择清洗首尾空白字符，快速获取纯净文本内容，解决本地文本读取与基础处理的需求。

# 能力边界
## 可处理
- 支持 [具体可处理的场景，如：本地 UTF-8 编码的 .txt、.md 纯文本文件]
- 支持 [具体功能，如：自定义是否清洗首尾空白字符]
## 不支持
- 不处理 [具体不支持的场景，如：二进制文件、加密文件、压缩包、远程网络文件]
- 不支持 [具体不支持的功能，如：超大文件分片处理、多编码自动适配]

# 前置依赖
1.  推荐运行环境：使用 uv run 执行脚本（无需手动安装依赖，脚本内嵌依赖声明）
2.  Python 版本：>=3.10
3.  无需手动安装第三方依赖，scripts/main.py 采用 PEP723 内嵌依赖，运行时自动隔离环境

# 可用脚本
-  **scripts/main.py**：当前技能的唯一 AI 调用入口脚本，提供完整的命令行参数和帮助文档，支持 --help 查看用法

# 调用工作流
## 命令行调用方式（AI/开发者均可使用）
在 Skill 根目录执行以下命令：
```bash
# 查看脚本帮助（必写，AI 靠此理解参数）
uv run scripts/main.py --help

# 常规调用示例（替换 [] 为实际参数）
uv run scripts/main.py [必要参数] --[可选参数]
```

## AI 调用约束
1.  仅允许调用 scripts/main.py，不直接访问 skill_name/ 内部源码包
2.  严格遵循 --help 提示的参数格式传参，不自定义非法参数
3.  **成功时**仅解析 **stdout** 的 JSON；**失败时**不向 stdout 索取结果，错误说明与（若有）错误 JSON 均在 **stderr**
4.  调用失败时，依据 stderr 中的错误提示或错误 JSON 修正入参后重新调用

# 评测信息
本技能内置标准化评测用例，用于验证 AI 调用稳定性和输出质量：
- 评测配置文件：evals/evals.json
- 评测样本文件：evals/files/（若有）
可通过 Agent 评测流程，运行评测用例验证技能可用性。输出质量分级与迭代方法见 [Evaluating skill output quality](https://agentskills.io/skill-creation/evaluating-skills)（可与 [Best practices for skill creators](https://agentskills.io/skill-creation/best-practices) 配合使用）。

# 开发者使用指引
## 本地工程安装
在 Skill 根目录执行以下命令，即可本地安装该 Python 包：
```bash
pip install -e .
```

## Python 原生调用
```python
# 导入核心函数（替换 [] 为实际函数名）
from [skill_name].core import [核心函数名]

# 调用示例（替换 [] 为实际参数）
result = [核心函数名]([参数1], [参数2])
print(result)
```
```

### 2. scripts/main.py（必选，AI 入口脚本）
#### 填充模板（直接复制，替换 [] 内内容即可）
```python
# /// script
# dependencies = [
#   # 填写脚本依赖的第三方包，无依赖则留空，格式："包名==版本号"
#   # 示例："beautifulsoup4>=4.12,<5"
# ]
# requires-python = ">=3.10"
# ///

import argparse
import json
import sys
from typing import Any, Dict

# 导入核心业务函数（替换 [] 为实际包名和函数名）
from [skill_name].core import [核心函数名]


def run():
    # 初始化命令行参数解析器（AI 靠此理解参数）
    parser = argparse.ArgumentParser(
        description="[填写脚本功能描述，与 SKILL.md 技能概述一致]",
        formatter_class=argparse.RawTextHelpFormatter
    )

    # 方式 A：参数化（适合功能差异小的场景，如 --action split|merge|extract）
    # parser.add_argument(
    #     "--action",
    #     choices=["split", "merge", "extract"],
    #     default="extract",
    #     help="操作类型 (默认: extract)"
    # )

    # 方式 B：子命令（适合功能差异大的场景，推荐）
    # subparsers = parser.add_subparsers(dest="command", required=True)
    # split_parser = subparsers.add_parser("split", help="拆分 PDF 页面")
    # merge_parser = subparsers.add_parser("merge", help="合并多个 PDF")

    # 添加必要参数（替换 [] 为实际参数信息）
    parser.add_argument(
        "[参数名]",
        help="[参数说明，如：目标文本文件路径]"
    )
    # 添加可选参数（根据业务需求增减，无则删除）
    parser.add_argument(
        "--[可选参数名]",
        action="store_true",
        default=True,
        help="[可选参数说明，如：是否清洗首尾空白字符 (默认: 开启)]"
    )

    # 解析参数
    args = parser.parse_args()

    try:
        # 调用核心业务逻辑（替换 [] 为实际参数，与 core.py 函数参数一致）
        # 若使用子命令，此处根据 args.command 分发到不同函数
        result = [核心函数名](
            [参数1]=args.[参数名1],
            [参数2]=args.[参数名2]
        )

        # 结构化 JSON 输出到 stdout（固定格式，不修改）
        output: Dict[str, Any] = {
            "success": True,
            "data": result
        }
        print(json.dumps(output, ensure_ascii=False))
        sys.exit(0)  # 成功退出码

    except Exception as e:
        # 人类可读与结构化错误均仅写入 stderr，失败时 stdout 不输出 JSON（与本仓库根目录全局约束一致）
        err_payload: Dict[str, Any] = {"success": False, "error": str(e)}
        print(f"Error: {str(e)}", file=sys.stderr)
        print(json.dumps(err_payload, ensure_ascii=False), file=sys.stderr)
        sys.exit(1)  # 错误退出码


if __name__ == "__main__":
    run()
```

### 3. evals/evals.json（必选，基础评测用例）
#### 填充模板（直接复制，替换 [] 内内容即可）
```json
{
  "skill_name": "[skill-name]",  # 与技能根目录名一致（短横线命名）
  "evals": [
    {
      "id": 1,
      "prompt": "[测试提示词，如：解析 evals/files/test.txt 文件，清洗首尾空白]",
      "expected_output": "[预期输出描述，如：返回去除首尾空白后的文本内容，stdout 输出 JSON 格式，success 为 true]",
      "files": ["evals/files/[测试文件名，如：test.txt]"]  # 无测试文件则删除 files 字段
    },
    {
      "id": 2,
      "prompt": "[第二个测试提示词，如：读取文本文件，不清洗首尾空白]",
      "expected_output": "[预期输出描述，如：返回原始文本内容，不做空白清洗，stdout 输出 JSON 格式，success 为 true]",
      "files": ["evals/files/[测试文件名，如：test.txt]"]  # 无测试文件则删除 files 字段
    }
  ]
}
```

### 4. skill_name/__init__.py（必选，导出核心函数）
#### 填充模板（直接复制，替换 [] 内内容即可）
```python
# 仅导出 core.py 中的核心函数，不写任何业务逻辑
from [skill_name].core import [核心函数名1], [核心函数名2]

__all__ = ["[核心函数名1]", "[核心函数名2]"]  # 明确导出的函数，方便外部导入
```

### 5. skill_name/core.py（必选，共享基础设施）
#### 编写规范
1.  纯原生 Python 代码，遵循 PEP8 规范，添加完整类型提示和 Google 风格文档字符串
2.  函数接口面向普通开发者设计，不考虑 AI 调用，不做任何适配
3.  业务逻辑完整，包含异常处理，不依赖 scripts/ 目录的任何代码
4.  当 skill 含多个子功能时，`core.py` 存放共享基础设施（加载、校验、错误处理、类型定义）；各子功能拆分为独立模块（如 `split.py`、`merge.py`），由 `core.py` 提供共享能力

#### 示例（以 file-parser 为例）
```python
from typing import Optional

def read_and_parse_file(file_path: str, strip_whitespace: bool = True) -> str:
    """读取本地 UTF-8 文本文件，并可选清洗首尾空白字符。

    Args:
        file_path: 目标文本文件的本地路径（绝对路径/相对路径均可）
        strip_whitespace: 是否清洗文本首尾的空白字符（空格、换行、制表符），默认 True

    Returns:
        str: 处理后的文本内容

    Raises:
        FileNotFoundError: 当传入的文件路径不存在时抛出
        UnicodeDecodeError: 当文件不是 UTF-8 编码时抛出
        PermissionError: 当没有文件读取权限时抛出
    """
    try:
        with open(file_path, "r", encoding="utf-8") as f:
            content = f.read()
        if strip_whitespace:
            content = content.strip()
        return content
    except FileNotFoundError:
        raise FileNotFoundError(f"文件不存在：{file_path}")
    except UnicodeDecodeError:
        raise UnicodeDecodeError(f"文件编码错误，仅支持 UTF-8 编码：{file_path}")
    except PermissionError:
        raise PermissionError(f"没有文件读取权限：{file_path}")
```

### 6. pyproject.toml（必选，Python 包配置）
#### 填充模板（直接复制，替换 [] 内内容即可）
```toml
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "[skill-name]"  # 与技能根目录名一致（短横线命名）
version = "0.1.0"
authors = [
  { name="[你的名字/团队名]", email="[你的邮箱，可选]" }
]
description = "[与 SKILL.md 中 YAML description 同意图：祈使+边界，≤1024 字符]"
requires-python = ">=3.10"
dependencies = [
  # 填写 core.py 依赖的第三方包，无则留空
  # 示例："beautifulsoup4>=4.12,<5"
]

[project.urls]
"Homepage" = "[可选，填写项目地址]"
"Bug Tracker" = "[可选，填写 bug 反馈地址]"
```

### 7. README.md（必选，开发者文档）
#### 填充模板（直接复制，替换 [] 内内容即可）
```markdown
    # [skill-name]
    [与 SKILL.md description 一致：何时用、何时不用，面向人读可略扩写]

    ## 功能介绍
    [详细说明功能用途、适用场景，与 SKILL.md 技能概述一致，可适当扩展]

    ## 安装方式
    在项目根目录执行以下命令，本地安装该 Python 包：
    ```bash
    pip install -e .
    ```

    ## 快速使用（原生 Python 调用）
    ```python
    # 导入核心函数
    from [skill_name].core import [核心函数名]

    # 调用示例
    result = [核心函数名]([参数1], [参数2])
    print(result)
    ```

    ## 参数说明
    | 参数名 | 类型 | 是否必填 | 默认值 | 说明 |
    | ------ | ---- | -------- | ------ | ---- |
    | [参数1] | [类型，如 str] | 是 | - | [参数说明] |
    | [参数2] | [类型，如 bool] | 否 | [默认值] | [参数说明] |

    ## 异常说明
    | 异常类型 | 触发条件 | 处理建议 |
    | -------- | -------- | -------- |
    | [异常类型，如 FileNotFoundError] | [触发条件，如文件路径不存在] | [处理建议，如检查文件路径是否正确] |

    ## 测试
    1.  单元测试：进入 tests/ 目录，运行测试脚本，验证业务逻辑正确性
    2.  AI 技能评测：通过 evals/evals.json 中的用例，验证 AI 调用稳定性
```

### 8. examples/ 目录（可选）
- 存放 .py 脚本文件，每个脚本对应一个具体的使用场景
- 脚本中包含完整的调用示例，注释清晰，方便开发者快速上手
- 示例：examples/basic_use.py（基础使用示例）、examples/edge_case.py（边界场景示例）

### 9. tests/ 目录（必选）
- 存放单元测试脚本，推荐使用 pytest 框架
- 每个测试脚本对应 core.py 中的一个函数，覆盖正常场景、边界场景、异常场景
- 示例：tests/test_core.py
```python
from [skill_name].core import [核心函数名]
import pytest

def test_normal_case():
    # 正常场景测试
    result = [核心函数名]([正常参数])
    assert result == [预期结果]

def test_edge_case():
    # 边界场景测试
    result = [核心函数名]([边界参数])
    assert result == [预期结果]

def test_exception_case():
    # 异常场景测试
    with pytest.raises([异常类型]):
        [核心函数名]([异常参数])
```

## 三、使用注意事项
1.  所有文件命名严格遵循规范：根目录短横线、源码包下划线，不混用
2.  `SKILL.md`、`scripts/main.py`、`evals/`、`pyproject.toml`、`README.md` 的文件名与相对位置固定不变；`skill_name/` 内可按功能拆分为多模块
3.  脚本和业务逻辑完全分离，scripts/main.py 不写任何业务代码，仅做参数解析、命令分发、结果输出
4.  两套测试体系独立：tests/ 测代码逻辑，evals/ 测 AI 调用，不混用
5.  所有 Markdown 文件、Python 文件均遵循语法规范，避免渲染/运行错误
6.  **默认与文档一致**：对可选行为须选定默认（`argparse` 默认值、`--help` 描述、`description` 与能力边界）并在各处统一，避免「多选一却无默认」
7.  **能力边界中的 Gotchas**：补充环境级、易错事实（例如软删数据须带某过滤条件、某 health 端点不代表 DB 就绪），见 [Best practices - Gotchas](https://agentskills.io/skill-creation/best-practices)

## 四、生成后自检清单（建议）
- 在技能根目录执行 `python -m compileall skill_name scripts`（路径按实际调整）无报错
- `uv run scripts/main.py --help` 可运行，参数与 `SKILL.md` 调用工作流一致
- **成功路径**：stdout 仅为 `success: true` 的 JSON；**失败路径**：stdout 无 JSON，错误信息或错误 JSON 仅在 stderr
- `evals/evals.json` 可被 JSON 解析且至少含 2 条 `evals` 用例
- 若已生成 `tests/`，`pytest`（或项目约定命令）可通过核心用例

## 五、渐进披露（可选）
若 `SKILL.md` 正文过长：可在技能根目录使用 `references/` 存放附录，并**仅在**七个固定章节**内部**用一句话写明「出现何种情况时再读哪一文件」；不得新增第八个顶层章节名，与 [progressive disclosure](https://agentskills.io/specification#progressive-disclosure) 及本仓库根目录全局约束一致。